name: nfx-policy

services:
  # 前端服务
  frontend:
    build:
      context: ./servers/frontend
      dockerfile: Dockerfile
    container_name: NFX-Policy-Frontend
    restart: always
    ports:
      - "${FRONTEND_HOST}:${FRONTEND_PORT}:80"
    volumes:
      - ./books:/usr/share/nginx/html/books:ro
    networks:
      - nfx-policy
    # depends_on:
    #   - backend-api

  # 后端 API 服务（预留，暂时不实现）
  # backend-api:
  #   image: nginx:alpine
  #   container_name: NFX-Policy-Backend-API
  #   restart: always
  #   ports:
  #     - "${BACKEND_HOST}:${BACKEND_PORT}:80"
  #   networks:
  #     - nfx-policy
  #   command: >
  #     sh -c "echo 'Backend API placeholder - not implemented yet' > /usr/share/nginx/html/index.html && nginx -g 'daemon off;'"

networks:
  nfx-policy:
    name: nfx-policy
    driver: bridge

